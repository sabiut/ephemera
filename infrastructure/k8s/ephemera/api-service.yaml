apiVersion: v1
kind: Service
metadata:
  name: ephemera-api
  namespace: ephemera-system
  labels:
    app: ephemera
    component: api
spec:
  type: ClusterIP
  selector:
    app: ephemera
    component: api
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
